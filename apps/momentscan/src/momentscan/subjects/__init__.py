"""Subject constitution - builds WHAT the analysis is about, before any signal reads it.

The chain: attribute.py (depth vote -> rider_role: WHO is the main rider) ->
tubelets.py (detections x attribution x scene-phase -> the subject's spatio-
temporal tube; THE boundary contract: extractors read tubelets ONLY, never raw
detections) -> crops.py (the tube's pixels, persisted H.264 - data retention:
the source expires, the crop track survives).

Membership test: "does this change when the SUBJECT CONTRACT changes (who /
which tube / which pixels), not when a model or a signal policy changes?"
Deliberately elsewhere: detect (a signal-extraction specialty - it OBSERVES;
its tracks are raw material, constituted here) - extraction/; stitch (identity-
domain cosine-merge policy, consumed by detect) - domains/.
"""
