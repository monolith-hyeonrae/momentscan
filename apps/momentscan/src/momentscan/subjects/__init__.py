"""Subject constitution - what the signals attach to: attribute (rider_role) ->
tubelets (THE boundary contract: extractors read tubelets, never raw detections)
-> crops (the tube's pixels). Membership: "does it change when the subject
contract changes?" See ARCHITECTURE.md."""
