"""Signal extractors — one research specialty per module; ``ls`` = the running
signal-analysis list. The secret: pure MEASUREMENT (a model observing an already-
extracted stream) — detect/tracking left for subjects/ in T6 (대상 확립은 측정이 아니다,
접수 #9). Dual-membership confession: media.py / ingest.py are I/O PLUMBING, not
measurement specialties — they live here but serve subjects/ too (crops reads media;
ingest is the Layer-0 spine). Membership: "is this a signal-processing specialty (a
model observing)?" Boundaries/isolation ladder/graduation: see ARCHITECTURE.md."""
